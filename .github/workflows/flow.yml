name: First Workflow
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: First Step
        run: ec "hello World"
        shell: bash
  second-job:
    runs-on: ubuntu-latest
    needs: first-job
    steps:
      - name: Good bye
        run: python -c 'print("Goodbye World")'
        shell: bash
