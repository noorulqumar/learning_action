name: First Workflow
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: First Step
        run: echo "hello World"
        shell: bash
  second-job:
    runs-on: ubuntu-latest
    steps:
      - name: Good bye
        run: python -c 'print("Goodbye World")'
        shell: bash
